# Reload config in development if files change
#Rails.application.config.to_prepare do

  #ThreadCurrentCleaner.configure do |config|
    #config.variable_names = [
      #'username',
      #'user_id',
      #'request_id',
      #'remote_ip',
      #'user_agent'
    #]
  #end

#end
